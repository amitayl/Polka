class Transaction {
    constructor(isDeal) {
        createdAt: Date.now();

        isDeal: false
        // owner: {id, productId, isReviewed},
        // bidder: {id, productId, isReviewed},
    }
}